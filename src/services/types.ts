export type Company = {
  id: string;
  name: string;
  logo: string;
  mobileNumber: string;
  owner: string;
  type: string;
  address: string;
};

export type PaperType = {
  date: Date;
  realDate: string;
};
